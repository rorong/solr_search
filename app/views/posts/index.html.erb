<h1>Posts Search</h1>

<%= form_for index_path, method: :get do |f| %>
  <p>
    <%= f.label "Search for" %>
    <%= text_field_tag :q, params[:q] %>
    <%= submit_tag "Search" %>
  </p>
<% end %>

<%= render 'posts', posts: @posts %>
